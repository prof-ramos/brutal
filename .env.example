# Example environment variables for Brutal Astrology Blog

# Admin Authentication
# IMPORTANT: Change these values for production!

# Username for admin login
ADMIN_USERNAME=admin

# Password for admin login (use strong password or bcrypt hash for production)
# Generate bcrypt hash: https://bcrypt-generator.com/
ADMIN_PASSWORD=change_this_password

# JWT Secret for session tokens (generate with: openssl rand -base64 32)
# IMPORTANT: Must be changed for production!
JWT_SECRET=generate_a_random_secret_here

# Vercel KV (optional - for analytics and caching)
# Get these from: https://vercel.com/dashboard/stores
# KV_REST_API_URL=
# KV_REST_API_TOKEN=
# KV_REST_API_READ_ONLY_TOKEN=